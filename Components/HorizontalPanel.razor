@using LayoutDesigner.Interfaces
@using LayoutDesigner.Pages
@inherits LayoutComponent
@implements ILayoutComponent
@implements IDropAreaContainer

<DropArea StyleClass="drop-area-horizontal" PreviewMode="PreviewMode" DesignMode="DesignMode" @ref="DropArea"></DropArea>

@code {
	public DropArea? DropArea { get; private set; }
}